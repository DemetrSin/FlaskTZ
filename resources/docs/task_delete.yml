delete:
  summary: Delete a task by ID
  parameters:
    - in: path
      name: task_id
      type: integer
      required: true
  responses:
    200:
      description: Task deleted successfully
    404:
      description: Task not found
